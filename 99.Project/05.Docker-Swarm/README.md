A partir du fichier *docker-compose.yml* précédent, déployez l'application sur un cluster Swarm.

Vous utiliserez pour cela l'instruction suivante:

```
$ docker stack deploy -c docker-compose.yml myapp
```
